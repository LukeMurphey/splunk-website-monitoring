# This eventtype can be used to filter out alerts from the search
# For example, a value of "title=localhost" would stop alerts for the input with the title of localhost
[filter_website_monitoring_alerts]
search=FILL_THIS_SEARCH_IN