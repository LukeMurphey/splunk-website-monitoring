[website_availability_overview]
enableSched = 0
dispatch.earliest_time = -24h
dispatch.latest_time = now
search = sourcetype="web_ping" | stats sparkline(avg(total_time)) as sparkline_response_time avg(total_time) as avg_response_time max(total_time) as max_response_time latest(response_code) as response_code latest(_time) as last_checked latest(title) as title latest(total_time) as response_time by url | table title url response_code last_checked response_time avg_response_time max_response_time sparkline_response_time | `timesince(last_checked,last_checked)` | eval response_time=round(response_time, 2)." ms" | eval avg_response_time=round(avg_response_time, 2)." ms" | eval max_response_time=round(max_response_time, 2)." ms" 